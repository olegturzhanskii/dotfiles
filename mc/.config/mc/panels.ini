[Panels]
# NOTE:
# eza is aliased with -a, so hidden files are what this environment shows by default.
#
# Without this, Midnight Commander hides exactly the files a dotfiles repository is made of.
show_dot_files=true
